.. _apps-wsgi-client:

===============================
Wsgi Client
===============================

The :class:`.HttpWsgiClient` is a tool for interacting with wsgi callable
using the same API as the :class:`.HttpClient`.
